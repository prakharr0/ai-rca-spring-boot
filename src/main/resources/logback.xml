<configuration>

    <appender name="RING_BUFFER" class="rathi.prakhar.ai.rca.spring.boot.core.context.RingBufferLogAppender" />

    <root level="INFO">
        <appender-ref ref="RING_BUFFER" />
    </root>

</configuration>