spring:
  application:
    name: RcaTest
  ai:
    openai:
      api-key: ${OPENAI_KEY}
      chat:
        options:
          model: gpt-4o-mini
      embedding:
        options:
          model: text-embedding-3-small

ai:
  rca:
    enabled: true
    rag:
      enabled: true

logging:
  level:
    io.github.prakharr0: debug