<configuration>

    <appender name="RING_BUFFER" class="context.io.jvai.ai.rca.spring.boot.core.RingBufferLogAppender" />

    <root level="INFO">
        <appender-ref ref="RING_BUFFER" />
    </root>

</configuration>